## 题意简述：

给定一个字符串数组，将所含字母完全相同的字符串组合在一起。

---
## 思路 ：
首先对于所含字母相同的字符串，不难发现其排序后所得字符串是相同的。
我们可以通过哈希表，将每种**排序后的字符串**作为键，对应答案容器的下标作为值。

这样对于读入的字符串 str，判断哈希表里是否有 str 为键的值（这里存的就是答案数组的下标）
- 若有，在答案容器对应位置 push_back (str)
- 若无，在答案容器新一位存储 str，同时将哈希表更新